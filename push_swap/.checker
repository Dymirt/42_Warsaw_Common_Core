i=1
while [ $i -le 100 ]
do
    ARG=$(shuf -i 0-14900 -n 5 | tr '\n' ' ');
    RESULT=$(./push_swap $ARG | wc -l);
    AVERAGE=$(($AVERAGE + $RESULT));
	i=$((i + 1))
done

AVERAGE=$(echo "scale=2; $AVERAGE / 100" | bc)

echo "AVERAGE: $AVERAGE"
